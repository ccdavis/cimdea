mod conventions;
mod defaults;
mod ipums_data_model;
mod ipums_metadata_model;
mod layout;
mod query_gen;
mod request;

use std::collections::HashMap;
use std::sync::Mutex;

use query_gen::*;

use conventions::*;

fn main() {}
