mod conventions;
mod defaults;
mod ipums_data_model;
mod ipums_metadata_model;
mod query_gen;
use std::collections::HashMap;
use std::sync::Mutex;

use query_gen::*;

use conventions::*;

fn main() {}
